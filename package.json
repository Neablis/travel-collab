{
  "name": "travel-collab",
  "private": true,
  "packageManager": "pnpm@11.25.0+sha512.5cde925b4f075f725eb71fbae18a42ffe784524789f19b61c731cb8721ec28aaee160e01a8d5af4fedb2a42cdbf300efe23db356b0d4a17b4d63e11f8ab7c956",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "setup": "node scripts/setup-env.mjs",
    "state": "node scripts/state-digest.mjs",
    "session-metrics": "node scripts/session-metrics.mjs",
    "lanes": "node scripts/lane-probe.mjs",
    "candidates": "node scripts/candidates.mjs",
    "milestones": "node scripts/milestones.mjs",
    "milestone": "node scripts/milestone.mjs",
    "surface": "node scripts/surface-size.mjs",
    "arch": "depcruise --ignore-known --output-type err apps/web/src packages/*/src",
    "arch:baseline": "node scripts/arch-baseline.mjs",
    "arch:graph": "depcruise --collapse '^(packages/[^/]+|apps/web/src/server/[^/]+|apps/web/src/components/[^/]+|apps/web/src/[^/]+)' --output-type mermaid apps/web/src packages/*/src",
    "tier3": "node scripts/tier3-stamp.mjs",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm --filter web lint && node scripts/check-lint-wall.mjs && node scripts/check-color-wall.mjs && node scripts/check-case-collisions.mjs && node scripts/check-loading-wall.mjs && node scripts/check-docstring-wall.mjs && node scripts/check-ki-filenames.mjs && node scripts/check-ki-citations.mjs && node scripts/check-migration-journal.mjs && node scripts/check-singletons.mjs && node scripts/surface-size.mjs --check && pnpm arch",
    "test": "pnpm -r --if-present test && node --test \"scripts/**/__tests__/**/*.test.mjs\"",
    "test:int": "pnpm --filter web test:int",
    "test:int:if-db": "node apps/web/scripts/db-probe.mjs; probe=$?; if [ $probe -eq 0 ]; then pnpm test:int; elif [ $probe -eq 1 ]; then printf '\\n================================================================\\n  SKIPPED: integration tests did NOT run.\\n  No database answered at DATABASE_URL (reason above).\\n  Start it with `docker compose up -d`.\\n  CI runs them. A green `pnpm check` here is NOT a green CI.\\n================================================================\\n\\n'; else exit $probe; fi",
    "mutate": "node scripts/mutate.mjs",
    "redfirst": "node scripts/redfirst.mjs",
    "check": "pnpm typecheck && pnpm lint && pnpm test && pnpm test:int:if-db && node scripts/tier3-stamp.mjs record",
    "seed:verify": "SEED_VERIFY_REPORT=1 pnpm --filter @tc/fixtures test",
    "content:verify": "pnpm --filter web content:verify"
  },
  "devDependencies": {
    "@stryker-mutator/core": "^10.0.0",
    "@stryker-mutator/vitest-runner": "^10.0.0",
    "dependency-cruiser": "^18.4.0",
    "typescript": "^5.5.0"
  }
}
