### KI-2026-09-05-i — widget-layer debt: an unreachable `count{of}`, five dead vocabulary items, a property test speaking a retired vocabulary, a hand-listed prompt line, and an undocumented refresh model

- **Severity:** cleanup — five LOW findings, all CONFIRMED, none with a user-visible defect today. Bundled as one entry; strike a line as it lands and close when all five are struck.
- **Milestone:** **M14, carried (assigned 2026-09-24, KI pass)** — owned by M14 (the notebook/widget builder), not a gate box. Listed in `docs/milestones/M14-rich-layer.md` § *Parked 2026-09-24*.
- **Status 2026-09-24:** items 1, 3, 4 and 5 struck; item 2 is down to `ItemScope`, which waits on the repeat-template decision. The entry stays open for that one line.
- **Area:** `packages/pages/src/macros/primitives/single.ts:78-82`, `presets.ts:73-88`, `presets.test.ts:49-54`, `registry-types.ts:156,167,323`, `result.ts:22`, `registry.ts:54-64`, `registry.property.test.ts:83-104`, `packages/contracts/src/pages.ts:241-242`, `apps/web/src/server/ai/handleAskRequest.ts:1013`, `apps/web/src/components/pages/PageScreen.tsx:118-143`
- **Symptom / What happens:**
  1. ~~**[F-B05](../../reviews/2026-09-05-overnight-review/findings/F-B05-count-of-day-city-has-no-preset.md) — `count{of: "day" | "city"}` has no preset**, so "how many days / how many cities" is code only the AI can reach (through `primitiveCatalog()` and the prompt line). Two preset rows fix it; the guard is generalising `presets.test.ts:49` so every enum value of every `nonFilterParams` entry must appear in some preset. Same shape as KI-2026-09-02-d.~~ **Fixed 2026-09-24 (M14 T03).** `count.days` already existed; `count.cities` ("How many cities") added. `presets.test.ts` "reaches every value of every non-filter param" sweeps `primitiveCatalog().params`: a value is reached when a preset sets it, or omits it and renders identically (`count` with no `of` counts stops). `field` inputs are exempt — the manifest is their vocabulary and the field picker their control. Proof: without the new row it fails `count{of: "city"} has no preset`.
  2. **[F-B06](../../reviews/2026-09-05-overnight-review/findings/F-B06-dead-widget-vocabulary-reads-as-seams.md) — five dead vocabulary items read as seams:** the `days`/`trip` input types no dimension maps to, `UnboundNeeds.days` (so `MacroView.tsx:133-134`'s "no days set" branch is unreachable), `ItemScope` (`resolve` is never called with `item`), `resolveMacro` (test-only callers) and `MacroKind` (one comment). Each is a decision the next contributor has to make; `resolveMacro` and `MacroKind` can go now, the other three depend on whether the repeat-template row is the next ADR. **Decided 2026-09-24 (Mitchell): the `days` and `trip` input types are retired**, and with them `UnboundNeeds.days` and the unreachable *"no days set"* branch — picking days is `DaysFilter` over the `dates` dimension. Decision only; strike this item when the code lands (M14). `ItemScope` still waits on the repeat-template decision.
     **Landed 2026-09-24 (M14 T09/T03), all but `ItemScope`:** ~~the `days`/`trip` input types~~, ~~`UnboundNeeds.days` and `MacroView`'s "no days set" branch~~, ~~`resolveMacro`~~ (its tests moved to `renderMacro`) and ~~`MacroKind`~~ (contracts CHANGELOG 2026-09-24) are deleted. `UnboundNeeds` gained `field` in the same change, for the new `field` input. **`ItemScope` is the one line left on this item**, still waiting on the repeat-template decision.
  3. ~~**[F-B07](../../reviews/2026-09-05-overnight-review/findings/F-B07-resolver-property-test-uses-retired-vocabulary.md) — the resolver property test's generator speaks the retired v1 vocabulary** (`dayRef`/`dayId`/`dayNumber`), and every schema is `filterParams(...).strip()`, so every case parses to `{}` and the `unbound` path its comment claims to exercise is never reached. False since 2026-09-04. Totality still holds. The `witness` floor cannot see this, because the assertion count is unchanged — generate from `FILTER_VALUE_SCHEMAS` and assert an `unbound` outcome is observed.~~ **Fixed 2026-09-24 (M14 T03).** One arbitrary per `FilterDimension` (a mapped type, so a new dimension fails to compile), non-filter params from `primitiveCatalog()`, a test that every generated value parses against `FILTER_VALUE_SCHEMAS`, and every widget taking `day` must be seen answering `unbound`. Proof: with the old generator restored, `cost never reported unbound across 200 runs: expected [ 'empty', 'ok' ] to include 'unbound'`. The new generator also bound tags for the first time, which showed the trip arbitrary's activities had no `kind`/`tags` (the contract defaults both on parse); the arbitrary now carries them.
  4. ~~**[F-B08](../../reviews/2026-09-05-overnight-review/findings/F-B08-prompt-hand-lists-non-filter-params.md) — the assistant prompt hand-enumerates the non-filter params** that `primitiveCatalog()` already derives, beside the derived catalogue, with the `compose_page` precedent (the same hand-list going stale) recorded ten lines above it.~~ **Fixed 2026-09-24 (M14 T09).** `insertWidgetParamsRule(catalogue)` (`apps/web/src/server/ai/pageTools.ts`) builds the sentence from the catalogue `handleAskRequest` sends. It had gone stale: `stop.rows`' `only` was in the catalogue and not the sentence. Proof: the old sentence fails `pageTools.test.ts` with `expected [ 'attribute.field', 'count.of' ] to deeply equal [ 'attribute.field', 'count.of', …(1) ]`.
  5. ~~**[F-B04](../../reviews/2026-09-05-overnight-review/findings/F-B04-notebook-never-refreshes-trip-detail.md) — an open notebook fetches trip detail once and never refreshes it**, so a day deleted in another tab keeps rendering until reload (after reload the path is correct: `narrow` → `unbound("day")` → "that day was removed"). **This is not a notebook bug** — `TripProvider` fetches on mount only too, there is no `visibilitychange`, `EventSource`, `setInterval` or `useSyncExternalStore` anywhere in `apps/web/src`, and the one asymmetry is that `usePreferences`'s `user` *does* refresh. So a fix scoped to `PageScreen` would **create** the asymmetry the finding first described. Either put a focus/`visibilitychange` refetch in the shared trip source that both board and notebook consume — the notebook then inherits it through `MacroEditorContext`, which is already reactive and currently has nothing feeding it — or record the model ("trip detail is fetched on navigation") in ADR-012 so the next reader does not rediscover it.~~
     **Fixed 2026-09-24 (M14 T04), in the shared source.** Since this was filed, M13 link 2 built that source: `useTripBroadcast` (`components/trip/context/broadcast.ts`, ADR-049) polls the trip's head `seq` and says "the trip moved". **`PageScreen` now subscribes to it** — its cursor seeded from the history `TripProvider` caches, and on news it refetches `trip` and `globals`, never the page, so widget values re-resolve through `MacroEditorContext` in Reading and Editing alike while the document an author is typing into is untouched (a remote edit to the *document* stays `KI-2026-09-22-d`'s). No notebook-only mechanism: one seam, gated the same way for both surfaces. Doing so found the seam itself short of **ADR-049 Decision 2**, which says *"the visibility-regain poll runs regardless"* of member count — the code gated regain and timer on one flag, and a test asserted a solo trip never polls at all. They are now two gates (`enabled`, `interval`), the demo check moved inside the hook, and a window regaining **focus** also polls (two visible windows side by side fire no `visibilitychange`), with one poll in flight at a time. So: a co-traveller's edit arrives within one 2s interval; the same person's edit in another tab or window arrives when they come back to this one; navigating from the board remounts and reads fresh, as before. Proof: `broadcast.test.tsx` (*with no interval, polls only when the tab comes back*: `expected "vi.fn()" to be called 1 times, but got 0 times` with the old single gate), `TripProvider.test.tsx` (*adopts an edit from the same person's other tab…*), `PageScreen.test.tsx` *the trip moves under an open page* (`Unable to find an element with the text: 2 days` with the subscription disabled; `expected "vi.fn()" to be called 1 times, but got 2 times` when a refresh also re-read the page), and the walk `m14-notebook-widgets.spec.ts` *a co-traveller moves a stop and shifts the days…*.
- **Why not fixed here:** found by a read-only review; no code was changed by it.
- **Cross-reference:** KI-2026-09-02-d (item 1's precedent); proposed widget rules 5, 9 and 12 in `../../reviews/2026-09-05-overnight-review/README.md` §B; ADR-039 decision 5.
- **First noted:** 2026-09-05, overnight review stream B.
