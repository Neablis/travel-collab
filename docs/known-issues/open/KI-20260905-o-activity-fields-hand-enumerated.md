### KI-2026-09-05-o — activity fields are hand-enumerated in ~21 files; the compile-forced descriptor scheduled on 2026-08-28 was never built and was never filed

- **Severity:** correctness (a recurrence class that has already bitten three times, each time as a silently dropped field)
- **Area:** `packages/contracts/src/activity.ts:103-110,119-126,159-166`; `packages/domain/src/trip/state.ts:3-12` (hand-written `ActivityState`), `equality.ts:40-60` (per-field compare, whose own comment at `:46-52` says "if you add one to the contract, add it here in the same commit"), `hydrate.ts:73-92`, `detail.ts:226-241`, `evolve.ts:78-83,96-99`, `diff.ts:352-392`, `decide.ts:219-262`. A file scan finds **21 non-test files** enumerating all of `timeWindow/location/notes/anchors/kind/tags/cost`.
- **Symptom / What happens:** every new activity field must be added by hand at ~21 sites, and nothing goes red when it is not. The class has bitten three times with the same shape: KI-1 (day order), KI-54 (`city`/`countryCode` invisible to equality, so city-only edits were rejected as a no-op), and M18's editor sheet dropping `kind`/`tags`. `docs/milestones/M18-stop-kind.md:261-275` says "§6.1's activity-field descriptor refactor is the standing fix and has now earned its place"; the 2026-08-28 review made it its §6.1 headline and `docs/milestones/README.md:348-349` scheduled it "as one overnight batch".
- **Why not fixed here:** it was not built, and — the reason this entry exists — **it was in neither the known-issues register nor `TODO.md`**, so nothing carried it after the milestone doc that scheduled it went quiet. Found by a read-only review; no code was changed by it.
- **Suggested fix (as written in the 2026-08-28 review §6.1):** one `ActivitySnapshot` Zod object in contracts; command and event payloads as `.extend()`/`.pick()` of it; `ActivityState = z.infer<…>`; a mapped-type `FIELD_EQUAL: Record<keyof ActivitySnapshot, (a, b) => boolean>` in `equality.ts` so a new field is a **compile error** until every site is updated. Wire-identical — a CHANGELOG entry, no migration.
- **Cross-reference:** [F-E01](../../reviews/2026-09-05-overnight-review/findings/F-E01-activity-fields-hand-enumerated.md) (MEDIUM, CONFIRMED; also filed as F-F01 from the simplification angle); resolved KI-1, resolved KI-54; the 2026-08-28 review §6.1; `docs/milestones/M18-stop-kind.md:261-275`, `docs/milestones/README.md:348-349`.
- **First noted:** 2026-08-28 (scheduled, not filed); re-found and filed 2026-09-05, overnight review streams E + F.
