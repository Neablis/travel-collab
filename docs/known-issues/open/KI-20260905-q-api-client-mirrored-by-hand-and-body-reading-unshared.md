### KI-2026-09-05-q — the "typed API client" is 35 hand-mirrored wrappers plus a second client plus three raw fetches, and five routes 500 on malformed JSON because body reading has no shared helper

- **Severity:** correctness (the five 500s are a client error reported as a server fault, and each one is a Sentry event) + cleanup (the mirroring)
- **Area:** `apps/web/src/lib/apiClient.ts` (999 lines, 35 `fetch(` calls against 37 `route.ts` files, **35 byte-identical** `catch` blocks), `lib/pagesClient.ts` (a second client with its own helpers and its own mirrored totality suite), raw `fetch` at `app/(app)/page.tsx:109`, `components/board/LocationInput.tsx:27` (`app/sentry-example-page/page.tsx:81` deleted 2026-09-25 with KI-2026-09-05-f item 2); bare `safeParse(await request.json())` at `api/trips/route.ts:43`, `trips/[tripId]/pages/route.ts:23`, `pages/[pageId]/route.ts:20`, `commands/route.ts:18`, `commands/batch/route.ts:21`; the guarded `.catch(() => null)` form at `invites/route.ts:14`, `saved-days/route.ts:17`, `account/preferences/route.ts:72`; `apps/web/src/mocks/handlers.ts:15-16`
- **Symptom / What happens:**
  1. ~~**[F-E04](../../reviews/2026-09-05-overnight-review/findings/F-E04-five-routes-500-on-malformed-json.md) — five routes 500 on malformed JSON while three return 400.** `Request.json()` rejects on invalid JSON; in the bare form the rejection escapes the handler and Next returns 500 for a client error, and Sentry records it as a server fault. Zod failures *do* return 400 in all five, so the inconsistency is purely the body read. The access half of the route preamble is well centralised (`requireTripAccess`); body reading is not, which is why the split is 5 bare vs 3 guarded. The correct in-repo pattern already exists at `handleAskRequest.ts:297-302`.~~ **Done 2026-09-25 (overnight sweep).** `apps/web/src/server/readBody.ts` — `readBody(request, schema, invalid)` answers `{ error: Response }` (a 400) for a body that is not JSON *and* for one the schema refuses, or `{ data }`, the same `"error" in x` shape `requireTripAccess` returns. Re-grepped: the five bare routes were still the only bare ones (`api/trips`, `trips/[tripId]/commands`, `commands/batch`, `pages`, `pages/[pageId]` PATCH), and all five now read through it, each keeping its own 400 message. `/api/v1` was checked and left alone (its own envelope; its one body read is already guarded), as was `dev/content/playbooks` (its `request.json()` sits inside a `try` that answers 400). Reproduced first: `src/server/readBody.int.test.ts` POSTs `{ this is not json` to each of the five, and before the fix all five rejected with `SyntaxError: Expected property name or '}' in JSON at position 2 (line 1 column 3)` — called directly, that escaping rejection is what Next turns into the 500. After: 5/5 answer 400. Rule 3: removing the `.catch` inside `readBody` puts all five back to the same `SyntaxError`; restored, green.
  2. ~~**[F-F03](../../reviews/2026-09-05-overnight-review/findings/F-F03-ask-handlers-duplicate-body-ritual.md) — the two AI handlers duplicate that ritual verbatim** (`handleAskRequest.ts:296-305` and `:846-855`, byte-identical), and the demo refusal too (`:268-273`, `:831-836`). It is the best-tested instance of the helper F-E04 wants. **Do these two as one change.**~~ **Already done before 2026-09-25, found so by the overnight sweep.** The body ritual is no longer duplicated: both halves of `/ask` call the shared `capRawBody` + `parseRequest` in `server/assistant/admission.ts`, and `handleApplyProposalRequest` says so at the call site. They were deliberately NOT moved onto `readBody`: `/ask` caps the body on bytes *before* parsing it, which `request.json()` cannot do, and its 400 names the rule broken — moving it would change AI behaviour, not just its spelling. The demo refusal is still written twice (the `refuseDemoTrip` admission stage and the plain 403 at the top of `handleApplyProposalRequest`); that is inside the AI cluster M9 owns and was left.
  3. **[F-E03](../../reviews/2026-09-05-overnight-review/findings/F-E03-api-client-is-35-hand-mirrored-wrappers.md) — every new route costs a hand-written wrapper, a hand-written mock and a hand-copied try/catch.** The totality invariant ("a client helper never rejects") is guarded *per module*: `apiClient.test.ts:229-261` enumerates only its own exports — it cannot see `pagesClient.ts` (which had to grow its own copy of the suite after a reject bug on 2026-09-03) or any raw `fetch`, and nothing lints `fetch` outside the client. Two guideline sentences describe mock generation that does not exist.
     **~~Nothing lints `fetch` outside the client~~ — done 2026-09-25 (overnight sweep).** `apps/web/eslint.config.mjs` has a FETCH WALL: `no-restricted-globals` on `fetch` for `src/**` outside the server tree, `app/api`, the three client modules (`apiClient`, `pagesClient`, `savedNotebooksClient`), tests, `test-support` and `mocks`. It is `no-restricted-globals` rather than the suggested `no-restricted-syntax` because flat config replaces a rule's options with the last matching block's, and the element wall already owns `no-restricted-syntax` for every `.tsx` — a second block would have silently switched it off. `scripts/check-lint-wall.mjs` proves it (a bare fetch in a component rejected; the element wall still firing on the same file; server code left clean — 31 → 34 assertions), and switching the rule to `"off"` puts `LINT WALL BREACHED: fetch wall: a bare fetch in UI code correctly rejected was NOT flagged by no-restricted-globals` on the run. The grep found **19** raw fetches, not three. The Home page's (`app/(app)/page.tsx`) moved onto the existing `fetchTrips()`. Four carried a line disable because they are not app-API calls and never will be (three since KI-2026-09-05-f deleted the Sentry scaffolding page the same night): the Sentry scaffolding page, the invite landing's `HEAD` cookie refresh, Auth.js's `/api/auth/session`, and the `/api/v1` import (the public API's own envelope). **The other 14** — `LocationInput` (geocode), `TokensSection` ×4, `PlanSection` ×3, `PlansScreen` ×3, `GrantForm`, `GrantList`, `useAiEntitled` — are app-API calls with no client helper yet; each carries a disable naming this entry, so `grep -rn "KI-2026-09-05-q" apps/web/src/components` is the backlog the collapse below should empty.
- **Narrowed 2026-09-25 (overnight sweep), NOT resolved.** What is left is the client half of item 3: collapse `apiClient.ts`, `pagesClient.ts` and `savedNotebooksClient.ts` onto one `request<T>(path, init, parse)` core so totality is structural (one try/catch, not 35) and one totality suite sees every helper; then move the 14 disabled call sites above onto it and delete their disables; and the hand-written mocks in `apps/web/src/mocks/handlers.ts`.
- **Decision (2026-09-25 overnight sweep):** (a) `readBody` takes the route's own 400 message and uses it for both a non-JSON body and a schema failure, so no existing client sees a changed error string — rejected: a fixed "malformed request" for every route (changes five wire messages for no gain) and separate messages for the two failures (the routes never distinguished them). (b) The eleven routes already using the guarded `request.json().catch(() => null)` form were left on it: they already answer 400, and moving them is a spelling change across billing, admin and account routes with no bug behind it — rejected as scope. (c) The 14 app-API raw fetches were grandfathered with line disables rather than moved onto new client helpers, because a new helper each is exactly the hand-mirroring this entry's remaining half exists to stop; rejected: adding 14 wrappers now (grows the thing the collapse must undo) and a config-level file ignore list (would also exempt any *new* fetch added to those files).
- **Why not fixed here:** found by a read-only review; no code was changed by it.
- **Suggested fix:** a `readBody(request, schema)` in `apps/web/src/server/` returning the same `"error" in x` shape as `requireTripAccess`, used by all eight routes and both AI handlers; a `no-restricted-syntax` lint on `fetch` outside the two client modules (80% of the value at 2% of the cost); then collapse both clients onto one `request<T>(path, init, parse)` core so totality is structural — one try/catch, not 35.
- **Cross-reference:** [F-E03](../../reviews/2026-09-05-overnight-review/findings/F-E03-api-client-is-35-hand-mirrored-wrappers.md), [F-E04](../../reviews/2026-09-05-overnight-review/findings/F-E04-five-routes-500-on-malformed-json.md), [F-F03](../../reviews/2026-09-05-overnight-review/findings/F-F03-ask-handlers-duplicate-body-ritual.md); KI-22 (the envelope half of the same wire); KI-2026-09-05-x (the other "client error surfaces as a 500" half); KI-2026-09-05-u (the two guideline sentences to correct).
- **First noted:** 2026-09-05, overnight review streams E + F.
