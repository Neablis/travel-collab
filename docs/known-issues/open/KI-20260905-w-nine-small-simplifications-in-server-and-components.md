### KI-2026-09-05-w — nine small duplications and dead exports: a second statement of the tool sets, two pass-throughs, two command executors, a redundant wall, a dead gate, four `Queryable`s, a dead menu, and a misplaced module

- **Severity:** cleanup — nine LOW findings, all CONFIRMED, none with any user impact. Bundled; strike a line as it lands. Total is roughly 150 lines removed across 9 independent edits.
- **Area:** see each finding below.
- **Symptom / What happens:**
  1. **[F-F02](../../reviews/2026-09-05-overnight-review/findings/F-F02-offeredtoolnamesfor-is-a-second-statement-of-tool-sets.md)** — `offeredToolNamesFor`/`AskToolSet` (`handleAskRequest.ts:92-124`) is a second, production-unused statement of which tools a turn gets; the runtime builds `tools` from the three builders. `route.int.test.ts:337` asserts it against a restatement of its own implementation, so the test cannot fail for any reason the implementation does not share. ~35 → ~5 lines.
  2. ~~**[F-F04](../../reviews/2026-09-05-overnight-review/findings/F-F04-two-single-caller-passthroughs-in-write-path.md)** — `buildWriteTools()` is `return buildPlanningTools()` (and `:45` already calls the latter directly, contradicting the "named door" justification), and `flushPlanningBatch(_tripId, calls, actorId)` passes through with `_tripId` unused. ~20 → ~2 lines; add a note to ADR-033 decision 5, which names `flushPlanningBatch` as surviving — the intent was the pipeline, not the wrapper.~~ **Done (2026-09-25 overnight sweep) — mechanical only: no tool set, prompt or batch changes.** Half was already gone on this base: `buildWriteTools()` no longer exists in production (the name survives only as a local helper in `writeTools.test.ts`, and `buildPlanningTools()` was removed in P3). The other half held: `flushPlanningBatch(_tripId, calls, actorId, also?)` in `ai/planningTools.ts` was `return executeTripCommandBatch(calls, actorId, also)`. `commitProposal` now calls the executor directly, `planningTools.ts` (holding only that wrapper) is deleted, `writeTools.test.ts` mocks `@/server/commands` instead (its argument index moved from `[1]` to `[0]`; red-then-green: batching `.slice(0, 1)` of the commands turns `commits ONE batch` red with `expected [ … ] to have a length of 2 but got 1`), the stale `docstring-wall-baseline.json` line is dropped, and ADR-033 decision 5 carries the note. `writeTools.test.ts` + `tools/planning.test.ts` 73/73 before and after; `ask/apply/route.int.test.ts` 28/28 before and after; typecheck clean; docstring wall OK.
  3. **[F-F06](../../reviews/2026-09-05-overnight-review/findings/F-F06-two-command-executors-share-five-steps.md)** — the two executors in `commands.ts` spell five of their seven steps twice (load+fold, members+forbidden, append+conflict differing only in `origin`, project), and the `TripHistory` literal is built twice. The invariant-1 sequence is written twice, so a change to the append/projection tail must be made in both. ~32 lines.
  4. ~~**[F-F07](../../reviews/2026-09-05-overnight-review/findings/F-F07-sleep-wall-redundant-with-eslint.md)** — `scripts/check-sleep-wall.mjs` duplicates `playwright/no-wait-for-timeout`, which has been at **error** over `e2e/**` since 2026-09-02, across a directory with zero `waitForTimeout` calls. Two exemption spellings for one rule, plus an extra Node process per `pnpm lint`. (The finding's first draft claimed it had no test; it does — `scripts/__tests__/check-sleep-wall.test.mjs` — and the verifier narrowed the finding to "redundant".) Deleting it means adding a `lintFixture` case to `check-lint-wall.mjs` proving the ESLint rule fires, and fixing five now-inverted references.~~ **Done (2026-09-25 overnight sweep).** Reproduced the redundancy first: `e2e/` has zero `waitForTimeout` calls, and a stdin fixture `e2e/__probe__.ts` calling `page.waitForTimeout(500)` already draws `3:9 error Unexpected use of page.waitForTimeout() playwright/no-wait-for-timeout`. `check-lint-wall.mjs` now fixtures exactly that (`sleep wall: waitForTimeout in an e2e spec correctly rejected (rejected by playwright/no-wait-for-timeout)`), and `check-lint-wall.test.mjs` pins it three ways: the rule in the attribution list, the count 34 → 35, and the sabotage case — with the rule switched off the wall must print `LINT WALL BREACHED: sleep wall: … was NOT flagged by playwright/no-wait-for-timeout (fired instead: nothing)` and exit 1, which it does (4/4 pass). `scripts/check-sleep-wall.mjs` and its test are deleted and dropped from `pnpm lint`; the references in `AGENTS.md`, `docs/guidelines/testing.md` (now naming the `eslint-disable-next-line … -- <reason>` exemption), `docs/guidelines/ci-cost-and-capacity.md`, `apps/web/eslint.config.mjs`, `scripts/__tests__/check-color-wall.test.mjs`, `TODO.md` and `.claude/skills/write-a-test/SKILL.md` are corrected. Historical records (`docs/plans/**`, `docs/milestones/M11-…`, resolved KIs, the review) are left as history.
  5. ~~**[F-F08](../../reviews/2026-09-05-overnight-review/findings/F-F08-checkadmission-dead-and-header-lies.md)** — `checkAdmission` has no production caller (only its own tests), and `admission.ts:13-17` describes the code being "asked twice — advisorily by the `/signup` form", a path that was never wired. Not a hole — `redeemAdmission` is the real gate and is wired and tested including its concurrency case — but a security module's header describing a check that does not exist is worth deleting rather than leaving to be believed.~~ **Done (2026-09-25 overnight sweep).** Still dead on this base (grep: `admission.ts:214` definition, callers only in `admission.int.test.ts`). `checkAdmission` deleted and the header rewritten to say the gate is asked once, by `recordSignIn` through `redeemAdmission`, with no advisory pre-check. Its `describe("checkAdmission is advisory")` block went with it — each of its three cases is already asserted of `redeemAdmission` (MISSING/INVALID/SPENT, super code unset). The one other use (`leaves the token pending…`) now asserts a second holder is still admitted via `redeemAdmission(token, RIVAL)`; red-then-green: making the trip-invite branch revoke the token turns it red (`expected { admitted: false, reason: "INVALID_INVITE_CODE" } to deeply equal { admitted: true, via: "trip-invite" }`). `admission.int.test.ts` 18 → 15 green; `admission.test.ts` 12/12; typecheck clean. `docs/milestones/M11a-invite-gate.md`, which said the function "remains … exported and tested", now records the deletion.
  6. ~~**[F-F09](../../reviews/2026-09-05-overnight-review/findings/F-F09-small-duplicates-in-server-ai.md)** — `usageOf` is byte-identical in `askAnalytics.ts` and `askIntent.ts` (which already imports from the former), and `modelIdOf` exists twice. ~12 lines. (The draft's "three truncators" clause was **dropped** — they differ in behaviour.)~~ **Done (2026-09-25 overnight sweep) — mechanical only: no tool set, prompt or record field changes.** Still present on this base, moved: `usageOf` in `ai/askIntent.ts` and `assistant/askAnalytics.ts` (same body), `modelIdOf` three times (`ai/askIntent.ts`, `ai/eval/transcript.ts`, and the exported one in `assistant/admission.ts`). `askAnalytics.usageOf` is now exported and `askIntent` uses it; both `modelIdOf` copies import the `assistant/admission.ts` export (the cycle `askIntent`'s comment cited — "that module imports this one" — no longer exists). Red-then-green: nulling `inputTokens` in the shared `usageOf` turns `askIntent.test.ts > records the classification's own cost and latency` red (`- "inputTokens": 151, + "inputTokens": null`), proving the classifier now reads the one copy. `askIntent`/`askAnalytics`/`assistant/admission` unit tests 90/90 before and after; `ai/eval/replay.int.test.ts` 41/41; typecheck clean.
  7. ~~**[F-F10](../../reviews/2026-09-05-overnight-review/findings/F-F10-queryable-declared-four-times.md)** — the identical `Queryable` type is declared in four modules, one with an apology, because `db/client.ts` exports only `db` and `Db`. ~9 lines.~~ **Done (2026-09-25 overnight sweep).** `db/client.ts` had since gained the export and four modules used it; the last three private copies (`eventStore.ts`, `projections.ts`, `savedDayAdds.ts` — the last with the apology) now import it. Type-only; proof is `pnpm --filter web typecheck` clean before and after.
  8. ~~**[F-F11](../../reviews/2026-09-05-overnight-review/findings/F-F11-accountmenufromsession-dead-and-comments-stale.md)** — `AccountMenuFromSession` has no production caller, and `AppHeader.tsx:11-14` plus two test comments name it as what `AppHeader` renders, which is `HeaderSessionChrome`. ~8 lines and three comments.~~ **Done (2026-09-25 overnight sweep).** `AccountMenuFromSession` deleted; `AppHeader.tsx`, `AppHeader.test.tsx` (two) and `AccountMenu.tsx` (two) comments now name `HeaderSessionChrome`. Its four tests were retargeted onto `HeaderSessionChrome` — the production entry point — rather than deleted; red-then-green: `callbackUrl: "/"` in `AccountMenuFor` turns `resolves the session client-side and dispatches the real signOut on click` red (`expected … { callbackUrl: '/welcome' } … Received "/"`). `AccountMenu.test.tsx` + `AppHeader.test.tsx` 23/23 before and after; typecheck clean.
  9. **[F-F12](../../reviews/2026-09-05-overnight-review/findings/F-F12-geocodenamematch-lives-in-ai-but-only-seed-uses-it.md)** — `server/ai/geocodeNameMatch.ts` (220 lines + 266 of tests) is imported only by `scripts/geocode-japan-seed.mts`, so anyone auditing `server/ai/**` reads 486 lines no request touches. **Do not move it before resolving KI-15's F-G02 note** — the answer there is that live enrichment *should* be calling it, in which case it stays and gains a caller.
- **Why not fixed here:** found by a read-only review; no code was changed by it.
- **Cross-reference:** `../../reviews/2026-09-05-overnight-review/README.md` §F, which also records **eight** places in `server/**` where complexity is deliberate and should **not** be simplified, and two patterns worth a small PR (pure data helpers exported from `.tsx`; `lib/playbooks.ts` and `lib/cities.ts` both saying "this belongs in `packages/contracts`"). ADR-033 decision 5 (item 2); KI-15 (item 9).
- **First noted:** 2026-09-05, overnight review stream F.
