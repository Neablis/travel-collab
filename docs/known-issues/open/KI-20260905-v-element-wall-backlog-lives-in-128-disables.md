### KI-2026-09-05-v — the element wall's real backlog is 128 line-level disables, 71 of them excusing geometry the rule cannot express, while `design-wall-pending.json` says `[]`

- **Severity:** cleanup (no defect; the wall works — it just charges a directive and a hand-written reason for one class it was never taught)
- **Area:** `apps/web/eslint.config.mjs:253` (the `JSXAttribute[name.name='style']` selector), `:224-227` (the exceptions policy), `:33` (`reportUnusedDisableDirectives: "error"` — the healthy half); `apps/web/src`: exactly **128** `eslint-disable-next-line no-restricted-syntax`, **75** `style={{` across 29 files, **71** directives immediately preceding a `style={{`; `scripts/design-wall-pending.json` is `[]`
- **Symptom / What happens:** the reason census is 30 "KI-2026-09-02-b: pre-existing, grandfathered" and the rest variations on "computed geometry, no token expresses it". So the wall keeps excusing one class it cannot express, every geometric style costs a directive plus prose, and the prose drifts — KI-2026-09-03-b is a directive citing a constraint that does not apply to `apps/web` at all. The tracking file that is supposed to hold the backlog is empty while the backlog lives in comments, so `ls` tells you nothing.
- **Why not fixed here:** found by a read-only review; no code was changed by it.
- **Suggested fix:** teach the rule the class — allow `style` when **every** property is geometric (`width|height|top|left|right|bottom|inset|transform|gridTemplateColumns|gridColumn|flexBasis|--*`) via an AST selector, and keep the disable for colour and typography. `reportUnusedDisableDirectives: "error"` then forces removal of the ~71 directives that become unused, so the backlog shrinks mechanically rather than by a sweep.
- **Cross-reference:** [F-E09](../../reviews/2026-09-05-overnight-review/findings/F-E09-design-wall-backlog-lives-in-128-disables.md) (LOW, CONFIRMED counts); KI-2026-09-02-b (the *test-quality* wall's separate 169-violation backlog, 30 of whose reason strings appear here), KI-2026-09-03-b (one drifted reason, and the evidence that they drift); KI-48.
- **First noted:** 2026-09-05, overnight review stream E.
