### KI-77 — The geocoder's name check rejects three correct venues on tokenisation, and the overlay silently loses them — RESOLVED
- **Severity:** cleanup (no product impact — `trip.ts` is canonical since ADR-030 and still carries 72/72 coordinates; this shrinks a cross-check, it does not move a pin)
- **Area:** `apps/web/src/server/ai/geocodeNameMatch.ts` (`nameTokens`, `distinctiveTokens`)
- **Symptom:** `placeNameVerdict` requires every distinctive token of the queried place to appear as a token of the candidate's own name, and `nameTokens` splits on every non-alphanumeric character. So a hyphen the vendor renders differently is a mismatch, and a translated suffix is a mismatch. Three of the Japan seed's stops are rejected this way — all three are the right place:
  ```
  "Gonpachi Nishiazabu"  vs  "Gonpachi Nishi-Azabu"   required [gonpachi, nishiazabu], candidate [gonpachi, nishi, azabu]
  "Tenryū-ji"            vs  "Tenryū Temple"          required [tenryu, ji],           candidate [tenryu, temple]
  "Ginkaku-ji"           vs  "Ginkakuji"              required [ginkaku, ji],          candidate [ginkakuji]
  ```
- **How it surfaced:** the 2026-08-29 regeneration for KI-58. These three had been in the overlay as `not-comparable` — the vendor had previously answered in local script (権八 西麻布, 天龍寺, 銀閣寺), which the check cannot read and therefore accepts. This run it answered in romaji, which the check *can* read and therefore rejects. **The verdict for a correct venue depends on which script the vendor happens to answer in**, which is not a property anyone chose.
- **Why it matters more than three rows:** `verify.ts` only checks a canonical coordinate where the overlay has an opinion, so each false rejection quietly removes one stop from the cross-check. It fails safe — nothing wrong is stored — but the guard covers 41 of 72 stops instead of 51, and nothing says so at the point of use.
- **Fix path:** compare on a concatenation-insensitive fold as well as a token fold, so "nishiazabu" matches "nishi"+"azabu" and "ginkakuji" matches "ginkaku"+"ji". That covers two of the three. "-ji" vs "Temple" is a translation, not a spelling, and wants either a small suffix synonym set (`-ji`/`-dera` → temple, `-jingū`/`-gū` → shrine) or acceptance that a translated name is `not-comparable` rather than `mismatch`. **Do not** simply widen `GENERIC_TOKENS` — that module's own comment warns a long list shrinks the distinctive set toward nothing.
- **Do not fix by loosening the caller.** The script's step 4b (KI-58) is a ranking; this is about what counts as a mismatch at all, and it belongs in the shared module with tests, not in the one-off script.
- **Found by:** the KI-58 fix, 2026-08-29.
- **Cross-reference:** KI-58 (the run that exposed it), KI-39 (which added the check), KI-15.
- **First noted:** 2026-08-29.
- **Resolved:** 2026-08-30, in `apps/web/src/server/ai/geocodeNameMatch.ts` only — no fixture, no coordinate and no caller changed. Three additions, each with its own comment in the module:
  1. `tokenRuns` + `runsCover` — the token cover is now computed over concatenations of *contiguous runs* of tokens on both sides, so a separator the vendor places differently is not an identity difference ("Gonpachi Nishiazabu" vs. "Gonpachi Nishi-Azabu", "Ginkaku-ji" vs. "Ginkakuji"). A run always begins and ends on a token boundary, so this is still equality, not containment: `"shin"` still does not match `"Shinagawa"` and `"kichi"` still does not match `"KICHIRI"`.
  2. A whole-name **identity** path on the separator-insensitive fold (`nameTokens(place).join("") === nameTokens(candidateOwnName).join("")`), checked on the raw token list before any category noun is dropped. This is what accepts "Ginkaku-ji" ≡ "Ginkakuji" outright.
  3. `CATEGORY_SYNONYMS` — a six-entry map folding `ji`/`dera`/`tera` → `temple` and `jingu`/`jinja`/`taisha` → `shrine`, i.e. the KI's suggested suffix synonym set rather than the widening of `GENERIC_TOKENS` it warned against: every entry folds onto a category noun already in that set, so the set of *categories* is unchanged and only their spellings grow. `ya` (屋) is deliberately excluded and the exclusion is commented — including it would reduce "Yoshida-ya" to `["yoshida"]` and start accepting "Coffee Yoshida" and "Steak House Yoshida", two of KI-39's own wrong-venue rows.
- **The accepted trade, written down in the module:** because `ji` is now a category noun, "Tenryū-ji" requires only `["tenryu"]`, so a hypothetical "Tenryu Restaurant" in the same ward would read as a match. Accepted as *not a new risk* — "Tenryū Temple", the English spelling of the same name and the one the vendor actually returned, has behaved exactly that way since KI-39 made `temple` generic. The alternative (keeping `ji` distinctive) leaves the verdict for a correct venue depending on which language the vendor answered in, which is the defect this entry is about. Pinned by a test so the cost is visible if anyone revisits it.
- **Proven by:** `apps/web/src/server/ai/geocodeNameMatch.test.ts`, extended from 58 to 63 tests. A new table carries the three live rejections from the 2026-08-29 LocationIQ run verbatim (`d1-s3-dinner-at-gonpachi`, `d9-s2-arashiyama-and-tenryu-ji`, `d10-s1-ginkaku-ji`); all three asserted `mismatch` before the change and `match` after. A second 21-row table asserts every rejection the module's comment requires it to keep making (Kichi Kichi/KICHIRI, Bread & Espresso, Yoshida-ya, Sushi Yoshitake, Kegon Falls, Gion Nanba, Torishiki, Zentis Osaka, Shin-Osaka Station, Hama-rikyū Gardens) and passed both before and after. The two wrong-BRANCH overlay entries in `packages/fixtures/src/japan/coordinateOverrides.ts` matched by name before this change and still do — branch disambiguation remains the box's job.
- **Checks run:** `pnpm --filter web exec vitest run -c vitest.unit.config.ts src/server/ai/geocodeNameMatch.test.ts` (63 passed), the same for `scripts/geocode-japan-seed.test.ts` (7 passed), `pnpm --filter web typecheck`, `pnpm --filter web lint`. `LOCATIONIQ_API_KEY` is unset in this container, so the vendor was not re-called; the fixture rows above are the transcript of the real run.
