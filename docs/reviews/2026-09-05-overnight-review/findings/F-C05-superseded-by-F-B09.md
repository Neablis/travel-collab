# F-C05 — superseded

The page-write `v` finding (any `v` accepted, no migrate-on-write) was verified and is real, but the verifier of stream B found the same write path has a worse consequence — it entombs unknown nodes — and the fix is identical. Both are recorded in **F-B09**. This stub exists so the id stays stable in the README index.
