# F-E01 — Activity fields are hand-enumerated in ~21 files; the compile-time descriptor called for on 2026-08-28 and again in M18's retro was never built or filed

- **Stream:** E Maintainability (= F01 Simplifiable) · **Severity:** MEDIUM · **Confidence:** CONFIRMED (verified)
- **Area:** `packages/contracts/src/activity.ts:103-110` (AddActivity), `:119-126` (UpdateActivity), `:159-166` (`ActivityPayloadFields`); `packages/domain/src/trip/state.ts:3-12` (hand-written `ActivityState`), `equality.ts:40-60` (per-field compare; its own comment at `:46-52` says "if you add one to the contract, add it here in the same commit"), `hydrate.ts:73-92`, `detail.ts:226-241`, `evolve.ts:78-83,96-99`, `diff.ts:352-392`, `decide.ts:219-262`. A file scan finds 21 non-test files enumerating all of `timeWindow/location/notes/anchors/kind/tags/cost`.
- **What is wrong:** the class has bitten three times with the same shape — KI-1 (day order), KI-54 (`city`/`countryCode` invisible to equality → city-only edits rejected as no-op), and the M18 editor sheet dropping `kind`/`tags` (`docs/milestones/M18-stop-kind.md:261-275`: "§6.1's activity-field descriptor refactor is the standing fix and has now earned its place"). The 2026-08-28 review made it the §6.1 headline and `docs/milestones/README.md:348-349` scheduled it "as one overnight batch"; it was not built, and it is in neither `docs/known-issues/` nor `TODO.md`. The only mitigation is `packages/contracts/test/activity-payload-parity.test.ts`, which guards event-payload parity, not the domain or equality sites. **Still open since 2026-08-28.**
- **Suggested fix:** as written in the 2026-08-28 review §6.1: one `ActivitySnapshot` Zod object in contracts; command and event payloads as `.extend()`/`.pick()` of it; `ActivityState = z.infer<...>`; a mapped-type `FIELD_EQUAL: Record<keyof ActivitySnapshot, (a, b) => boolean>` in `equality.ts` so a new field is a **compile error** until every site is updated. Wire-identical, so a CHANGELOG entry but no migration.
- **Scope of the fix:** `packages/contracts/src/activity.ts` (+ CHANGELOG), `packages/domain/src/trip/{state,equality,hydrate,detail,evolve,diff,decide}.ts`. Contracts change → own PR (AGENTS.md rule). Check subset: contracts touched → `pnpm check` (the skill says do not narrow for contracts).
- **Test that should exist:** none new — the compiler becomes the test; the existing diff round-trip property, `hydrate.property.test.ts`, the equality suite and `commands.int.test.ts:229`'s rebuild equality are the behaviour-preservation net.
- **Cross-reference:** KI-1, KI-54 (both resolved), M18 retro, 2026-08-28 review §1.6/§6.1/§6.2, F-F01 (same finding from the simplification angle).
- **Do not:** fossilize `equality.ts`'s Location field list into the descriptor before deciding the 2026-08-28 §1.6 question (does `city`/`countryCode` equality matter?); do not do this in the same PR as any feature.
