# F-E09 — The element wall's backlog is 128 line-level disables, 71 of them excusing geometry the rule cannot express

- **Stream:** E Maintainability · **Severity:** LOW · **Confidence:** CONFIRMED (verified counts)
- **Area:** `apps/web/eslint.config.mjs:253` (selector `JSXAttribute[name.name='style']`), `:224-227` (exceptions policy), `:33` (`reportUnusedDisableDirectives: "error"` — the healthy mechanism); `apps/web/src`: exactly 128 `eslint-disable-next-line no-restricted-syntax`, 75 `style={{` across 29 files, 71 directives immediately preceding a `style={{`; `scripts/design-wall-pending.json` is `[]` while the real backlog lives in comments; reason census: 30 "KI-2026-09-02-b: pre-existing, grandfathered", the rest variations on "computed geometry … no token equivalent" (`MapRail.tsx` 11, `UnscheduledRack.tsx` 10, `TimelineLens.tsx` 9).
- **What is wrong:** the wall keeps excusing one class it cannot express, so every geometric style costs a directive and a hand-written reason, and KI-2026-09-03-b shows the reasons drift (a directive citing a constraint that does not apply).
- **Suggested fix:** teach the rule the class — allow `style` when every property is geometric (`width|height|top|left|right|bottom|inset|transform|gridTemplateColumns|gridColumn|flexBasis|--*`) via an AST selector, keep the disable for colour/typography; `reportUnusedDisableDirectives` will then force removal of the ~71 directives that become unused.
- **Scope of the fix:** `apps/web/eslint.config.mjs` element-wall block + the files whose directives become unused. Check subset: `pnpm --filter web lint`, `node scripts/check-lint-wall.mjs` (add a fixture: a colour in `style` still fails).
- **Cross-reference:** KI-2026-09-03-b, KI-48, KI-2026-09-02-b, `docs/guidelines/design-system.md`.
