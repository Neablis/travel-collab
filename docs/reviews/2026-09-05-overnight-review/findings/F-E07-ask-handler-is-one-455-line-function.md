# F-E07 — `handleAskRequest()` is one 455-line function, and the AI layer holds ~15 of 40 open KIs whose common fix (KI-9's schema-required gateway wrapper) has been agreed since July

- **Stream:** E Maintainability · **Severity:** MEDIUM · **Confidence:** CONFIRMED (verified; mostly a pointer to KI-9 — the god-function split is the net-new item)
- **Area:** `apps/web/src/server/ai/handleAskRequest.ts:244-699` (`handleAskRequest` body; the file is 1048 lines of which ~583 are comments recording incidents — do not trim those); `writeTools.ts:183` (`rest as unknown as BatchableCommand`, deliberate and commented); the flow map in the review README §Stream F lists the 13 steps and the file that owns each.
- **What is wrong:** KI-9 (validated ad hoc), 22 (envelope not in contracts), 80, 82, 93/94/97 are consequences of model output crossing into the system at many places with no single typed boundary. The deferred fix at KI-9 — a gateway wrapper that *requires* an output schema — would make a new un-parsed consumer a type error. Separately, the handler's 455 lines already name their own seams (`briefFor`, `pageInsertsMetadata`, `instructionsFor`, the recorder, the stream response) and every change to the AI route lands inside one function. Correction to the first draft: the `JSON.parse` sites at `:299` and `:854` are *guarded* (try → 400) and are the good example the rest of `app/api` should copy (F-E04).
- **Suggested fix:** land KI-9's wrapper; split `handleAskRequest` at the seams it already names into ~5 functions of <100 lines each, keeping the incident comments attached to the step they describe; move the stream envelope into `packages/contracts` (KI-22).
- **Scope of the fix:** `server/ai/*`, `packages/contracts` (KI-22 half → CHANGELOG). Check subset: `server/ai` unit + `ask/route.int.test.ts`, `ask/apply/route.int.test.ts`.
- **Cross-reference:** KI-9, KI-22, ADR-019/033, M9; F-F02/F03/F04/F09 (mechanical simplifications inside the same files — do them in the same PR as the split).
- **Do not:** duplicate the KIs; delete the comment record.
