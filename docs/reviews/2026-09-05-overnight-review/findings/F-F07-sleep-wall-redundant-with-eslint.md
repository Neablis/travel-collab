# F-F07 — `scripts/check-sleep-wall.mjs` duplicates an ESLint rule that has been at error since 2026-09-02, over a directory with zero sleeps

- **Stream:** F Simplifiable · **Severity:** LOW · **Confidence:** CONFIRMED as "redundant" (verified); the first draft's "has no test" claim was wrong — `scripts/__tests__/check-sleep-wall.test.mjs` exists
- **Area:** `scripts/check-sleep-wall.mjs` (91 lines, in `pnpm lint` at `package.json:12`) + `scripts/__tests__/check-sleep-wall.test.mjs`; `apps/web/eslint.config.mjs:296-318` (Playwright block scoped to `e2e/**/*.ts` at `:303`, every recommended rule promoted to `"error"` at `:312-317`, so `playwright/no-wait-for-timeout` is at error; `:309-311`'s rationale — "would have made a second, weaker copy of `scripts/check-sleep-wall.mjs`" — is now inverted); `apps/web/package.json:12` lint covers `e2e`; `rg waitForTimeout apps/web` → no hits.
- **What is wrong:** two exemption spellings for one rule (`// e2e-sleep-allowed:` for the script, `eslint-disable-next-line playwright/no-wait-for-timeout -- reason` for ESLint), one extra Node process per `pnpm lint`, and a script + test protecting against a pattern that no longer exists in the tree.
- **Suggested fix:** delete the script and its test; add one `lintFixture` case to `check-lint-wall.mjs` (pattern at `:225-236`) proving `playwright/no-wait-for-timeout` fires under `e2e/`; fix `eslint.config.mjs:309-311`, `scripts/__tests__/check-color-wall.test.mjs:9` ("unlike check-sleep-wall.mjs"), `package.json:12`, and the references in `docs/guidelines/testing.md` and `ci-cost-and-capacity.md`. State in the PR that justified sleeps now need an ESLint disable with a reason (`reportUnusedDisableDirectives: "error"` keeps them honest).
- **Scope of the fix:** `scripts/`, `scripts/__tests__/`, `package.json`, `eslint.config.mjs`, two guidelines. Check subset: `pnpm lint`, `node --test scripts/__tests__`.
- **Cross-reference:** KI-20260830 (resolved), KI-013/021/075 (the sleeps it was built against, resolved), F-E06.
