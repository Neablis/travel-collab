import { LandingScreen } from "@/components/front/LandingScreen";

export const metadata = { title: "Caesura — plan the trip together" };

export default function WelcomePage() {
  return <LandingScreen />;
}
